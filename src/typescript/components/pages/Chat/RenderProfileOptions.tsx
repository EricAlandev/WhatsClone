

type RenderProfileOptions = {
    options: any[];
} 

export default function RenderProfileOptions({options} : RenderProfileOptions){

    return(
        <>
            {options?.length > 0 && (
                <>
                    <p>
                        
                    </p>
                </>
            )}
        </>
    )
}